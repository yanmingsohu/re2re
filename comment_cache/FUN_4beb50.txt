<b style="color: #2e7d32;">函数功能概括</b>

该函数是一个**组内默认对象（Index 0）的合法性校验器**。它将输入的低 16 位参数进行位移和标志位注入，构造出一个复合 ID，并强制指定子索引为 `0`，通过调用 `FUN_4ed820` 来检索该组中的第一个对象是否有效。

<b style="color: #1565c0;">详细分析</b>

### 1. 逻辑流程图解
函数接收一个参数（通常是 Group ID 的低位部分），对其进行位变换后，作为复合参数传递给校验函数，并配合硬编码的 `0` 作为子索引。

<code style="display: block; background-color: #f5f5f5; padding: 10px; border-radius: 5px;">
// 伪代码实现
bool FUN_4beb50(uint32_t input_val) {
    // 1. 获取输入参数并进行位操作
    // AH 是 EAX 的第 8-15 位
    uint32_t modified_val = input_val;
    modified_val |= (4 << 8); // 等价于 or AH, 4

    // 2. 构造复合 ID：将处理后的低 16 位移至高 16 位，低 16 位补 0
    uint32_t composite_id = (modified_val << 16);

    // 3. 调用校验函数 FUN_4ed820(composite_id, sub_index)
    // 其中 sub_index 被硬编码为 0
    return FUN_4ed820(composite_id, 0);
}
</code>

### 2. 关键指令与寄存器变换分析

| 指令 | 操作对象 | 详细解释 | 对结果的影响 |
| :--- | :--- | :--- | :--- |
| `mov EAX, [ESP+4]` | `EAX` | 从栈中取出第一个参数（假设为 `Group_Raw`） | `EAX = Group_Raw` |
| `or AH, 4` | `AH` (EAX bits 8-15) | 将输入参数的第 8-15 位与 `0x04` 进行按位或操作 | **注入标志位**：在 ID 中设置特定的属性/类型标记 |
| `shl EAX, 16` | `EAX` | 将整个 32 位寄存器左移 16 位 | **重构结构**：将原本的低 16 位变成复合参数的高位，符合 `FUN_4ed820` 的解析逻辑 |
| `push 0` | Stack | 将常数 `0` 压栈 | 设置 **Sub-Index = 0** (即获取该组的首个元素) |
| `call FUN_4ed820` | PC/IP | 调用底层校验函数 | 执行实际的查找与状态验证逻辑 |

### 3. 参数构造数学模型
假设输入参数为 $X$：
$$\text{Argument}_1 = (X \lor 0x00000400) \ll 16$$
$$\text{Argument}_2 = 0$$

---

<b style="color: #c62828;">与之前分析的对比</b>

| 特性 | `FUN_4ed820` (依赖项) | `FUN_4beb50` (当前函数) |
| :--- | :--- | :--- |
| **功能定位** | 通用的双层索引校验（底层） | 针对“组内首个对象”的快速检查（高层封装） |
| **参数来源** | 完全由调用者提供复合 ID 和子索引 | 自动构造复合 ID，并固定子索引为 `0` |
| **复杂度** | 高（涉及查找表、状态位判断） | 低（仅进行简单的位运算和转发） |

---

<b style="color: #ef6c00;">额外洞察与建议</b>

*   **对象结构猜测**：
    由于该函数通过 `or AH, 4` 修改了 ID，这表明输入的原始参数可能不仅仅是单纯的数字，或者这个 `0x04` 是某种 **"Default Object" 或 "Group Master" 的类型标识符 (Type Flag)**。在校验时，系统会根据这个标志位来决定如何解释该组内的对象。
*   **可能的用途**：
    这种模式常见于游戏引擎中管理“容器”或“层级”的逻辑。例如：`FUN_4beb50` 可能是 `IsGroupDefaultObjectValid()` 的实现，用于检查某个物理组、渲染层或 AI 行为组中的主对象是否还活着。
*   **后续分析方向**：
    1.  **追踪标志位含义**：观察是否有其他函数也对 `AH` 进行类似的 `or` 操作（例如 `or AH, 1` 或 `or AH, 2`），这能帮你还原出完整的对象类型枚举。
    2.  **观察返回值**：通过调试查看 `FUN_4ed820` 返回的是布尔值（0/1）还是一个指向对象的指针，从而确定此函数的最终用途是“是否存在”还是“获取该对象”。